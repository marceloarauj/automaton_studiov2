import 'package:flutter/material.dart';

class LoadProjectView extends StatelessWidget {
  const LoadProjectView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("LoadProject"));
  }
}